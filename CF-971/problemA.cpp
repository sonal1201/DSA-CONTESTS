// Minimize

#include <iostream>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int a, b;
        cin >> a >> b;
        int c = (b + c) / 2;
        int ans = (c - a) + (b - c);
        cout << ans << endl;
    }
}